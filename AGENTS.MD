# Automation 仓库协作约定

## 基本原则

- 代码注释、技术文档和提交说明使用简体中文；标识符、API、JSON 字段保持英文。
- 平台开发只维护一条当前契约。修改模型、Schema、运行时、Bridge/MCP、编辑器时同步更新整条链路，并删除被替代的旧入口、字段、分支和说明。
- 只为真实职责、状态或不变量建立抽象。单页面稳定 UI 逻辑可以留在页面，不按文件行数或目录对称强行拆分。
- 保留用户已有的无关改动；只修改当前任务需要的范围。

## 获取上下文

- 只读取与任务直接相关的源码和文档；已知精确边界时直接读源码，不批量加载 Markdown。
- 架构入口按范围选择：启动 `01`、配置 `02`、流程/Engine `03`、编辑器 `04`、AI/Bridge `05`、运动安全 `06`；目录职责不清楚时再读对应 README。

## 核心架构边界

- 单个平台实例由 `AutomationPlatformHost` 持有 `PlatformRuntime`；Store、Engine、设备、安全和编辑会话通过实例显式传递，不恢复静态全局容器或静态窗体引用。
- HMI 只通过 `IAutomationPlatform` 使用平台能力，不直接引用平台运行时、窗体、Store、Engine 或原生运动对象。
- 窗体负责布局、确认、事件转发和渲染；跨页面状态、配置事务、设备协议、运行闸门和复用规则进入现有 Workspace、Store 或应用服务。
- 流程配置内存、`Work/` 事务和运行控制分别由 `ProcessDefinitionRepository`、`ProcessWorkDirectoryTransaction`、`IProcessRuntimeControl` 负责。手工编辑和 AI 复用现有流程/变量配置服务。
- `Automation.Runtime.Contracts` 保持无 UI、无实现依赖；没有实际复用或依赖问题时不新增程序集和装饰接口。
- 配置路径统一来自 `PlatformRuntime.Paths`，运行时释放统一进入 `PlatformShutdownCoordinator`。

## 运行、配置与安全

- 流程未结束时重复启动应明确失败并保留当前实例；涉及设备或人员风险的不确定状态优先停机、报警。
- 保存只检查结构与 `saveRequired`；`ProcessReadinessService` 和启动闸门检查 `runRequired`。缺少运行资源可保存为 `incomplete`，但不能启动。
- 缺失配置按当前契约生成并持久化默认值；已有值格式错误、越界或违反约束时严格失败，不静默猜测或修复。
- 非致命服务异常应降级完成平台/HMI 初始化；流程配置故障必须停止流程、设置运行闸门并禁止启动。
- 辅助诊断开关不得改变流程状态、安全停止、基础报警或失败日志。

## AI、MCP 与 Bridge

- 流程结构写入只走 ChangeSet V2：`preview_change_set` → 用户确认 → `apply_change_set(previewId)`；不得恢复旧 intent/patch/create_batch 或旁路写入。
- `Automation.Protocol/AutomationToolProfiles.cs` 是任务 Profile 精确工具名集合的权威来源；`McpServer/McpToolProfile.cs` 负责权限外壳、过滤和 Schema 收窄。公共参数使用 `Automation.Protocol` 强类型 DTO 和生成 Schema。
- `Diagnostic/Editor` 只作为用户权限外壳；每次普通 AI 请求先把本请求的 Goose 原生会话切到只开放 `TaskCoordinator.request_capability` 的工具面。`request_capability` 只申请一个下一能力，第一条成功申请立即锁定；当前能力足以完成或需要用户信息时模型直接正常回复，不把结果包装进控制工具。模型根据新事实决定是否换能力，不预生成整单阶段计划，不使用关键词路由或规划失败兜底，也不把多个 Profile 合并成大工具集。
- 能力申请只表达模型意图，不授予权限。代码校验 Profile、权限外壳、完全权限和合法迁移；普通流程与资源写入依赖预演确认，不由逐字授权引文硬拦截。运行控制、平台配置和源码修改仍必须逐字引用当前用户消息中的授权片段，历史、附件和模型输出不能单独扩大授权。缺失或不合法的申请不执行副作用，并把可行动的结构化错误交回模型修正，不按固定纠错次数停止。审查进入副作用能力前必须取得当前状态读取证据；设计知识按当前目标需要读取，不建立首次必读或完成闸门；运行前必须验证最近流程 ChangeSet、平台 migration 或资源 `configurationSaved`。只有失败结果不能证明无副作用时才标记不安全部分写入；无副作用预演失败后成功提交视为已恢复。
- 同一用户请求内的能力切换通过 Goose 会话扩展接口原地替换 Automation MCP 与内置扩展并保持 `sessionId`；切换后必须读取 Goose 实际工具目录，同时核对完整 Automation 工具名集合和 `request_capability` 输入 Schema 指纹。请求到达完成、停止或失败终态后标记可信滚动，下一次用户请求创建新的原生会话并只注入有限最终消息、结构化交接和机械阶段事实；业务 `conversationId` 继续保持。业务历史只持久化最终输出；分析草稿、工具过程、大型工具结果和重复总结不得跨请求积累。
- 源码只读定位优先使用受根目录、扩展名和结果数约束的 `search_platform_source`，配合 `read/tree` 不触发重建；direct `write/edit` 视为已修改，`SourceDevelopment` 中执行 Shell 视为修改状态不确定，两者都必须重新构建并加载新版本后再续做。复合任务部分完成后保留最后有效输出和副作用事实，停止或失败时不得自动重放整单请求。
- Schema 描述结构，`OperationBehaviorCatalog` 描述运行语义，Readiness 描述启动阻塞；不要用提示词掩盖代码契约问题。面向模型的复合写入 Schema 应复用共享字段并用紧凑的类型/字段映射说明各动作，不把同一递归对象复制成大量 `oneOf` 分支；逐类型必填、禁用和一致性仍由 MCP/Bridge/编译器机械校验并返回可行动问题。
- `saveRequired` 与 `runRequired` 保持分层；提交后使用稳定 `procId/stepId/opId`，预演局部 `key` 不跨提交阶段。
- 只读评审只有在需要把已证明 finding 交给后续修复时才调用独立的 `submit_review_handoff`；普通评审答复可直接完成，宿主会为缺失交接保存不授权修复的 `unresolved`。finding 必须逐项引用宿主从成功工具结果机械提取的事实，且只有 `repairability=safe_without_external_facts` 的已证明 finding 能直接作为评审修复依据。设计缺口、仍需用户选择、未决项和未证明缺陷不得被后续阶段升级为可直接修复的事实。
- `config.placeholder` 编译为一等不可执行占位指令，可用计划成功/失败出口表达草稿控制流；它只占位未知动作或结果，`Note/message` 不能证明其中描述的重试、超时、清理或出口已经实现，也不得复用弹框、延时或其他真实指令制造运行假象。占位仍为占位时可用 `operation.update` 修订名称、说明和计划出口；确认真实动作后必须用 `operation.replace` 完整替换。清空字符串使用 `variable.clear`，不把 `variable.set value=""` 同时解释成缺参和清空。
- 用户只指定成功/失败出口时，只建立对应控制流，不自动推导报警、停机、复位、重试、提示等额外副作用；现有资源或常见工艺也不能代替用户意图。当前 ChangeSet 内的 `operationKey` 先按当前步骤解析，跨步骤全局唯一时可直接解析，只有同名歧义时才要求 `stepId/stepKey`；稳定 `operationId` 是权威目标，冗余步骤信息不得造成无意义失败。
- 新建流程直接进入 `ProcessCreate`，按目标需要读取设计知识并用 `resolve_authoring_inputs`、`resolve_operation_capability` 取得必要事实，再使用创建专用 `preview_change_set`；不要先绕行纯方案能力，也不增加第二套流程声明 DSL。`ProcessCreate` 仅使用 `process.create`、`step.append` 和 `operation.append`，并用当前 ChangeSet 局部 key 串联新流程、步骤、指令和流程变量。简单且事实充分时可一次完整创建；复杂、带回环或资源未决时，先提交 `autoStart=false` 且可独立审查和保存的安全功能块，再转入 `ProcessEdit` 按稳定 ID 续建；原子性只约束当前提交的数据一致性，不要求复杂目标一次写完。重试优先使用真实指令已有的 `RetryCount`；需要流程级重试时，模型直接使用变量复位/累加、分支和跳转表达业务语义，平台不为表面形状增加自动分类或生成宏。入口不可达等尚未完成的控制流进入 Readiness，允许以 `incomplete` 保存但禁止启动；悬空引用、不可编译目标和原子一致性错误仍拒绝保存。既有流程修改继续使用原子 ChangeSet V2。
- 修改原生/语义指令、Schema、资源类型、ChangeSet 或 AI 前台时，同步检查模型、编译、行为、校验、运行和测试；只读取该任务需要的 `05-AI与Bridge.md` 章节。

## Prompt、Skill 与可信输出

- AI 能力建设遵循“给模型铺路，不替模型思考”：宿主负责提供目的明确且趁手的工具、紧凑可信的当前事实、可行动的错误与恢复信息、必要的领域说明，以及不可绕过的权限和安全边界；模型在当前授权能力内自主决定读取顺序、分析深度、阶段拆分和工具组合。推荐工作流只描述目标、可用能力和完成证据，不固化为固定调用顺序、固定轮数或过程模板；新增外围机制必须证明它让正确路径更短或更可靠，否则不增加。
- 运行时规则按职责放置：System Prompt 只保存跨任务稳定原则，`automation.md` 保存平台路由和证据边界，Skill 保存任务工作流，结构、行为、状态和合法迁移由代码契约提供；不要在多个运行时提示层复制同一规则。
- Prompt 是正式能力契约的一部分。对需要模型进行语义判断、歧义处理、风险取舍或事实措辞的问题，允许加入1～3句简短且可泛化的规则；一次可靠测试若暴露一类稳定问题即可作为依据，不必等待重复失败。能够由 Schema、状态机、权限或数据完整性契约确定性保证的约束仍下沉代码，安全边界不得只依赖 Prompt；不为单个措辞追加案例补丁。
- 新增模型行为硬限制前，必须证明它守住的是可机械验证的不变量，并说明正向收益、误杀风险以及为什么不能通过更好的工具、Schema、状态机或错误反馈解决。工具调用数、输出分段数、能力阶段数、是否立即调用工具、自然语言目标重复、固定纠错次数和耗时只可作为诊断信号，不得据此推断模型没有思考进展或自动中断；权限、安全、事实与证据完整性、事务一致性、数据/传输边界和用户可配置的真实资源预算除外。
- 轨迹阈值默认只用于观测和优化定位，不得在没有上述论证时升级为执行闸门。Prompt 与 Skill 优先描述完成条件、可用能力和证据缺口，不用“立即结束”“只能尝试若干次”等措辞迫使模型过早收敛。
- 纯方案设计不进入写入 Skill或枚举当前资源；只读流程审查与流程写入使用独立 Skill，只读请求不推导写入或运行控制授权。占位写入使用 `config.placeholder` 保持 `incomplete`，不得用延时、弹框或猜测资源伪造成可运行。领域方法放在 `Assets/Goose/automation.md` 与对应 Skill；能力申请和合法迁移只在代码契约维护。
- AI 契约必须保留原始事实并区分事实、推断和证据缺口；汇总不得替代明细，分页不得丢项或混合不同修订。精确字段、数量边界和错误码只在 Schema、Bridge/MCP 与 `05-AI与Bridge.md` 维护。
- 优先给模型提供面向目的的聚合工具：单流程评审先用 `inspect_process` 一次取得概览、Readiness、流程图、流程引用和有界字段详情，再按真实缺口钻取；不要让模型拼接固定的基础读取套餐。聚合结果仍必须保留来源路径和机械证据绑定。
- 修改受管 Prompt、平台上下文或 Skill 时，同步更新资源版本、嵌入/输出复制、启动部署、内容校验、运行分析和契约测试；同版本内容漂移恢复内置内容，高于内置版本的本机文件按当前部署策略保留并报告。
- 架构文档只描述当前程序；一次性交接、阶段计划和工作区快照完成接管后删除，不让历史状态与当前契约长期并存。

## 验证与日志

- 默认只运行与改动直接相关、能够快速结束的精准测试，并优先编译受影响项目；不要把全量测试当作常规收尾步骤。
- 预计单项或整组验证超过 2 分钟时，执行前先取得用户确认；测试超时后停止并报告，不自动重跑或改跑更大的测试集合。
- `dotnet test Automation.sln -c Debug -p:Platform=x64`、HMI 综合回归和真实 AI 标准测试仅在用户明确要求，或共享底层契约存在无法由短测试覆盖的高风险时执行；后一种情况也必须先说明原因并取得确认。没有发布配置特有风险时不重复跑 Release。
- 架构回归只保护真实依赖方向、退役入口和安全边界，不设置文件行数等机械指标。
- 配置、状态机和安全改动应验证失败无副作用、必要回滚和重复调用结果；仅编译通过不算完成。
- 运行日志统一写入 `D:\AutomationLogs\` 的既有分类目录，不写程序执行目录。
