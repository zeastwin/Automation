# Automation 项目开发上下文

`Hmi/` 是自动化项目源码的可修改范围，包括 WinForms 人机界面和 `CustomFunc.cs`。`Stores/` 中的公开类型及 `Runtime/AutomationPlatformHost` 是项目开发可调用的接口；实现时读取其实际公开签名。平台其余源码可用于理解接口，但不是项目代码的修改目标。

流程和运行配置通过 `automation` 工具操作，不直接编辑 `Config` JSON。C# 自定义函数不支持运行时热加载：可以写完整代码和流程，但应提示用户自行编译、重启后再运行，不要代替用户重启正在运行的平台。

# 项目偏好

自动化开发工程师可在以下文件中补充当前项目偏好：

@AGENTS.md
